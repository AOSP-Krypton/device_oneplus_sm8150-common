android_app {
    name: "OnePlusCameraHelper",

    srcs: ["src/**/*.java", "src/**/*.kt"],

    certificate: "platform",
    platform_apis: true,
    privileged: true,

    resource_dirs: ["res"],

    optimize: {
        proguard_flags_files: ["proguard.flags"],
    },

}